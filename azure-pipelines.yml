trigger:
- main

variables:
  azureServiceConnectionId: 'bd4c03a2-a046-4b60-8476-1f1b389dda82'
  webAppName: 'susharithaweb'
  pythonVersion: '3.12'

stages:

- stage: Build
  displayName: Build Stage
  jobs:
  - job: BuildJob
    pool:
      name: TestAgent   # ✅ Self-hosted pool

    steps:
    - script: |
        echo Python Version
        python --version
      displayName: Check Python

    - script: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
      displayName: Install Dependencies

    - task: ArchiveFiles@2
      inputs:
        rootFolderOrFile: '.'
        includeRootFolder: false
        archiveType: zip
        archiveFile: $(Build.ArtifactStagingDirectory)/app.zip
        replaceExistingArchive: true
      displayName: Archive Files

    - publish: $(Build.ArtifactStagingDirectory)/app.zip
      artifact: drop

- stage: Deploy
  displayName: Deploy Stage
  dependsOn: Build
  condition: succeeded()

  jobs:
  - deployment: DeploymentJob
    pool:
      name: TestAgent   # ✅ Self-hosted pool again

    environment: swarnawebapp

    strategy:
      runOnce:
        deploy:
          steps:
          - task: AzureWebApp@1
            inputs:
              azureSubscription: $(azureServiceConnectionId)
              appType: webAppLinux
              appName: $(webAppName)
              package: $(Pipeline.Workspace)/drop/app.zip
